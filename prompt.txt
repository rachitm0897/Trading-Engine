Read `./trading_engine_implementation_plan.md` completely before making any changes.

Audit the current repository, then implement the plan phase by phase in the listed order. Treat the plan as the main source of scope, priorities, acceptance criteria, testing requirements, and documentation requirements.

Do not add user authentication, user-specific flows, tenant logic, live trading, or split the application into additional deployment services.

Keep all trading paper-only. Make small, logical changes, add tests for every behavioural fix, delete genuinely unused legacy code instead of preserving compatibility layers, and do not weaken existing tests.

Run all checks and tests specified in the plan. Fix failures caused by your changes. Record completed work, deleted code, migrations, performance improvements, exact test results, remaining failures, and deferred items in `docs/backend-audit-results.md`.

Continue until the plan’s acceptance conditions are satisfied or a blocker cannot be safely resolved. Document any blocker and the exact reason instead of guessing or hiding the failure.
::: 