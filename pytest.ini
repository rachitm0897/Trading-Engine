[pytest]
DJANGO_SETTINGS_MODULE = config.test_settings
python_files = test_*.py
markers =
    docker_integration: requires the real Docker Compose Kafka, Flink, PostgreSQL, Redis, Backend, and paper Gateway services
